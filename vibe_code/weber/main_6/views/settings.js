export function renderSettingsView(parent) {
    parent.innerHTML = `
        <div class="container">
            <h1>Settings</h1>
            <p>App settings and preferences.</p>
        </div>
    `;
}